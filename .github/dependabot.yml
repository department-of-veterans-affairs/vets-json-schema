version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/" # Location of package manifests
    schedule:
      interval: "weekly"
      time: "09:00"
      timezone: "America/New_York"
    cooldown:
      default-days: 7
    # Disable pull requests to prevent automatic vulnerable dependency updates
    open-pull-requests-limit: 0
