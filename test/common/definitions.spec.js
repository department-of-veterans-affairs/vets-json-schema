import { expect } from 'chai';
import definitions from '../../src/common/definitions';
import Ajv from 'ajv';
import _ from 'lodash';

describe('schema definitions', () => {
  it('foo', () => {
    console.log(definitions.fullName)
  });
});
